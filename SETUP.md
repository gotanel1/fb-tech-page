# คู่มือติดตั้ง — บอทโพสต์ข่าวเทคลงเพจ Facebook อัตโนมัติ

ใช้เวลาทั้งหมดประมาณ **30-45 นาที** ทำครั้งเดียวจบ หลังจากนั้นระบบจะโพสต์เองทุกวัน
โดยที่คุณไม่ต้องเปิดคอมพิวเตอร์เลย

---

## ระบบนี้ทำงานยังไง

```
GitHub Actions ตื่นตามเวลา (09:00 / 13:00 / 19:00 น.)
        ↓
ดึงข่าวจาก RSS 11 แหล่ง (The Verge, Ars Technica, Hacker News, Blognone ...)
        ↓
คัดข่าวที่ใหม่ที่สุด + ตรงกับแนวเพจที่สุด และข้ามข่าวที่เคยโพสต์ไปแล้ว
        ↓
Gemini อ่านข่าวเต็ม แล้วเขียนโพสต์ภาษาไทยตาม style.md
        ↓
โพสต์ขึ้นเพจผ่าน Facebook Graph API + ใส่ลิงก์ต้นทางในคอมเมนต์แรก
        ↓
จดไว้ใน state/posted.json ว่าโพสต์ข่าวนี้แล้ว (กันโพสต์ซ้ำ 45 วัน)
```

**ค่าใช้จ่าย: 0 บาท** — GitHub Actions ฟรีสำหรับ public repo และ Gemini มี free tier
ที่เหลือเฟือสำหรับวันละ 3 โพสต์

---

## ขั้นตอนที่ 1 — เอาโค้ดขึ้น GitHub

1. เข้า https://github.com/new สร้าง repo ใหม่ ตั้งชื่ออะไรก็ได้ เช่น `fb-tech-page`
   - เลือก **Public** (ถ้าเลือก Private จะมีโควตา Actions จำกัด 2,000 นาที/เดือน
     ซึ่งจริง ๆ ก็ยังพอใช้ แต่ Public ฟรีไม่จำกัด)
   - **ไม่ต้อง** ติ๊ก Add README

2. เปิด Terminal / PowerShell ในโฟลเดอร์นี้ แล้วรัน:

```bash
git init
git add .
git commit -m "เริ่มต้นบอทโพสต์ข่าวเทค"
git branch -M main
git remote add origin https://github.com/ชื่อคุณ/fb-tech-page.git
git push -u origin main
```

> ไฟล์ `.env` ถูกใส่ไว้ใน `.gitignore` แล้ว จะไม่หลุดขึ้น GitHub

---

## ขั้นตอนที่ 2 — ขอ Gemini API Key (2 นาที)

1. เข้า https://aistudio.google.com/apikey
2. ล็อกอินด้วยบัญชี Google
3. กด **Create API key** → เลือกโปรเจกต์ (หรือสร้างใหม่)
4. ก๊อปคีย์เก็บไว้ หน้าตาจะประมาณ `AIzaSy...`

---

## ขั้นตอนที่ 3 — สร้าง Meta App

> **หาเมนูไม่เจอ? ข้ามการกดเมนูไปเลย** — ก๊อปลิงก์นี้วางในเบราว์เซอร์ได้ตรง ๆ
> **https://developers.facebook.com/apps/creation/**
> มันจะพาเข้าหน้าสร้างแอปทันที ไม่ต้องไล่หาปุ่มไหนทั้งนั้น

> **หมายเหตุเรื่องภาษา:** หน้า Meta for Developers จะแสดงภาษาตามบัญชี Facebook ของคุณ
> แต่ Meta แปลไว้ไม่ครบ — เมนูหลักเป็นไทย ส่วนชื่อ permission และคำเฉพาะทางยังเป็น
> อังกฤษอยู่ ข้างล่างนี้ผมใส่ **ทั้งไทยและอังกฤษ** ไว้ให้ ให้ยึด**คำอังกฤษในวงเล็บ**
> เป็นหลัก เพราะอันนั้นตรงกับที่คุณเห็นบนจอแน่นอน
>
> ถ้าอยากให้จอตรงกับคู่มือทุกตัวอักษร เปลี่ยนภาษาบัญชีเป็นอังกฤษชั่วคราวได้ที่
> https://www.facebook.com/settings?tab=language แล้วค่อยเปลี่ยนกลับทีหลัง

### 3.1 สมัครเป็นนักพัฒนา (ถ้ายังไม่เคย)

ครั้งแรกที่เข้า มันจะให้ยืนยันเบอร์โทรและอีเมล กด **ถัดไป (Next)** ไปเรื่อย ๆ จนจบ

### 3.2 กรอกรายละเอียดแอป — หน้า "App details"

| ช่อง | ใส่อะไร |
|---|---|
| ชื่อแอป (**App name**) | อะไรก็ได้ เช่น `TechPage Bot` — **ห้ามมีคำว่า Facebook, Meta, Insta** ในชื่อ |
| อีเมลติดต่อ (**App contact email**) | อีเมลคุณ |

กด **ถัดไป (Next)**

### 3.3 เลือกกรณีการใช้งาน — หน้า "Use cases"

หน้านี้จะมีการ์ดให้เลือกเยอะมาก **เลือกอันเดียวคือ:**

> **จัดการทุกอย่างบนเพจของคุณ** — ภาษาอังกฤษเขียนว่า **"Manage everything on your Page"**
>
> คำอธิบายใต้การ์ดจะพูดถึงการโพสต์คอนเทนต์ ตอบคอมเมนต์ และดูสถิติ

เลือกแล้วกด **ถัดไป (Next)**

> ⚠️ **จุดที่คู่มือเก่า ๆ ในเน็ตทำให้หลง:** เมื่อก่อนหน้านี้จะให้เลือก "Other"
> แล้วค่อยเลือกประเภทแอปเป็น "Business" — **ตอนนี้ Meta เอาขั้นตอนเลือกประเภทแอปออกไปแล้ว**
> มีแค่การเลือก use case อย่างเดียว ถ้าคุณหาหน้า "Business / Consumer" ไม่เจอ
> ไม่ใช่คุณหาไม่เป็น แต่มันไม่มีแล้วจริง ๆ

### 3.4 หน้า "Business"

มันจะถามว่าจะผูกกับพอร์ตโฟลิโอธุรกิจไหม **เลือก:**

> **ฉันยังไม่ต้องการเชื่อมต่อพอร์ตโฟลิโอธุรกิจ**
> (**"I don't want to connect a business portfolio yet"**)

กด **ถัดไป (Next)**

### 3.5 หน้า "Requirements" และ "Overview"

อ่านผ่าน ๆ แล้วกด **ถัดไป (Next)** → หน้าสุดท้ายกด **ไปที่แดชบอร์ด (Go to dashboard)**

### 3.6 เก็บ App ID กับ App Secret

ในแดชบอร์ดแอป เมนูซ้ายมือ ไปที่
**การตั้งค่าแอป (App settings)** → **พื้นฐาน (Basic)**

> หาเมนูไม่เจออีก? ใช้ลิงก์ตรงก็ได้ — เอาเลข App ID ที่เห็นบนหัวหน้าเว็บมาแทน `APP_ID`
> `https://developers.facebook.com/apps/APP_ID/settings/basic/`

จดไว้ 2 ค่า:
- **App ID** (รหัสแอป) — ตัวเลขยาว ๆ
- **App Secret** (รหัสลับของแอป) — กด **แสดง (Show)** ก่อนถึงจะเห็น

> **ไม่ต้องส่ง App Review และไม่ต้องเปิดแอปเป็น Live** ปล่อยไว้โหมด
> **การพัฒนา (Development)** ได้เลย เพราะคุณเป็นแอดมินทั้งแอปและเพจ
> Meta อนุญาตให้ใช้ permission เต็มกับทรัพย์สินของตัวเองอยู่แล้ว

---

## ขั้นตอนที่ 4 — สร้าง User Token ชั่วคราว

เปิดลิงก์นี้ตรง ๆ: **https://developers.facebook.com/tools/explorer/**

หน้าตาเครื่องมือนี้เป็นภาษาอังกฤษเกือบทั้งหมด แม้บัญชีคุณจะเป็นไทย

1. **แถบขวามือ** ช่อง **Meta App** → เลือกแอปที่เพิ่งสร้าง
2. ช่อง **User or Page** → เลือก **User Token**
3. ในกล่อง **Permissions** ข้างล่าง กด **Add a Permission** แล้วพิมพ์ค้นหาทีละอัน
   ติ๊กให้ครบ **5 อัน** (ชื่อพวกนี้เป็นภาษาอังกฤษเสมอ ไม่มีแปลไทย):

   - `pages_show_list`
   - `pages_manage_posts`
   - `pages_read_engagement`
   - `pages_manage_engagement`
   - `pages_manage_metadata`

   > `pages_manage_engagement` คืออันที่ทำให้บอทคอมเมนต์ลิงก์ใต้โพสต์ตัวเองได้
   > ถ้าไม่ใส่ โพสต์จะขึ้นแต่คอมเมนต์จะไม่ขึ้น

4. กด **Generate Access Token**
5. หน้าต่างล็อกอินจะเด้งขึ้นมา — **ตรงหน้าที่ให้เลือกเพจ ต้องกดเลือกเพจของคุณจริง ๆ**
   (หน้านี้จะเป็นภาษาไทยว่า "เลือกเพจที่คุณต้องการใช้กับ...") อย่ากดผ่านเร็วเกินไป
   ถ้าไม่ได้ติ๊กเพจ ขั้นตอนที่ 5 จะขึ้นว่าไม่พบเพจ
6. ก๊อป token ยาว ๆ ที่โผล่มาในช่องด้านบน

   > token อันนี้อายุแค่ 1-2 ชั่วโมง ไม่ต้องตกใจ ขั้นตอนต่อไปจะแปลงให้ถาวร
---

## ขั้นตอนที่ 5 — แปลงเป็น Page Token ถาวร

ในโฟลเดอร์นี้ รัน:

```bash
pip install -r requirements.txt
python tools/get_token.py
```

สคริปต์จะถาม **App ID**, **App Secret** (จากขั้นตอนที่ 3) และ **User Token**
(จากขั้นตอนที่ 4) แล้วพ่นผลลัพธ์ออกมา 2 บรรทัด:

```
FB_PAGE_ID=1234567890
FB_PAGE_TOKEN=EAAxxxxxxxxxx...
```

**เก็บ 2 ค่านี้ไว้ให้ดี** — Page Token ที่ได้มาแบบนี้ไม่มีวันหมดอายุ
(จะหมดก็ต่อเมื่อคุณเปลี่ยนรหัสผ่าน Facebook หรือถอนสิทธิ์แอปเอง)

---

## ขั้นตอนที่ 6 — ใส่ Secrets ใน GitHub

ไปที่ repo ของคุณ → **Settings** → **Secrets and variables** → **Actions**

กด **New repository secret** เพิ่มทีละอัน รวม 3 อัน:

| ชื่อ | ค่า |
|---|---|
| `FB_PAGE_ID` | เลข Page ID จากขั้นตอนที่ 5 |
| `FB_PAGE_TOKEN` | Page Token จากขั้นตอนที่ 5 |
| `GEMINI_API_KEY` | คีย์จากขั้นตอนที่ 2 |

**(ไม่บังคับ)** ถ้าอยากปรับพฤติกรรมบอท ไปแท็บ **Variables** ข้าง ๆ กัน แล้วเพิ่ม:

| ชื่อ | ค่าเริ่มต้น | ความหมาย |
|---|---|---|
| `POSTS_PER_RUN` | `1` | โพสต์กี่ชิ้นต่อการรัน 1 รอบ |
| `MAX_AGE_HOURS` | `36` | ข่าวเก่าเกินกี่ชั่วโมงไม่เอา |
| `POST_STYLE` | `comment` | `comment` = ลิงก์อยู่คอมเมนต์แรก / `link` = แนบลิงก์ในโพสต์ |
| `GEMINI_MODEL` | `gemini-3.5-flash` | เปลี่ยนรุ่นโมเดลได้ |

---

## ขั้นตอนที่ 7 — ทดสอบ

1. ไปที่แท็บ **Actions** ใน repo
2. เลือก **Auto Post to Facebook Page** ทางซ้าย
3. กด **Run workflow** → ติ๊ก **ทดลองรันโดยไม่โพสต์จริง** → **Run workflow**
4. รอ 1-2 นาที แล้วกดเข้าไปดู log

ถ้าเห็นโพสต์ภาษาไทยที่ AI เขียนใน log แปลว่าทุกอย่างพร้อมแล้ว
คราวนี้กด **Run workflow** อีกรอบโดย **ไม่ติ๊ก** dry run เพื่อโพสต์จริงครั้งแรก

> ถ้าอยากทดสอบบนเครื่องตัวเองก่อน: ก๊อป `.env.example` เป็น `.env`
> กรอกค่าให้ครบ แล้วรัน `python src/main.py --dry-run`

---

## หลังจากนี้

**ไม่ต้องทำอะไรอีกเลย** ระบบจะโพสต์เองวันละ 3 ครั้ง เวลา 09:00 / 13:00 / 19:00 น.

---

## ปรับแต่งภายหลัง

| อยากเปลี่ยน | แก้ไฟล์ |
|---|---|
| โทน/บุคลิก/ความยาวของโพสต์ | `style.md` |
| แหล่งข่าว, คำที่ห้าม, คำที่อยากดันคะแนน | `feeds.yaml` |
| เวลาโพสต์ | `.github/workflows/post.yml` ส่วน `cron` |

แก้เสร็จแล้ว `git add . && git commit -m "ปรับสไตล์" && git push` เท่านั้นเอง

> **เรื่องเวลาใน cron:** ค่าที่เขียนเป็นเวลา UTC ถ้าอยากโพสต์กี่โมงเวลาไทย
> ให้ **ลบ 7** เช่น อยากโพสต์ 20:00 น. ไทย → เขียน `0 13 * * *`

---

## ปัญหาที่เจอบ่อย

**`(#200) Requires pages_manage_posts permission`**
ตอนสร้าง token ในขั้นตอนที่ 4 ไม่ได้ติ๊ก permission ครบ 5 อัน หรือไม่ได้ติ๊กเพจ
ในหน้าที่เด้งขึ้นมา ให้ย้อนกลับไปทำขั้นตอนที่ 4-5 ใหม่

**โพสต์ขึ้น แต่คอมเมนต์ลิงก์ไม่ขึ้น**
ขาด `pages_manage_engagement` ให้ทำขั้นตอนที่ 4-5 ใหม่โดยติ๊กอันนี้เพิ่ม
หรือจะเลี่ยงด้วยการตั้ง GitHub Variable `POST_STYLE` เป็น `link` ก็ได้

**หาเมนูในหน้า Meta for Developers ไม่เจอ**
ใช้ลิงก์ตรงแทนการไล่กดเมนู:
สร้างแอป https://developers.facebook.com/apps/creation/ ·
รายการแอป https://developers.facebook.com/apps/ ·
Graph API Explorer https://developers.facebook.com/tools/explorer/

**`Error validating access token: Session has expired`**
Token ไม่ได้ถูกแปลงเป็นแบบยาว — ต้องรัน `tools/get_token.py` ไม่ใช่เอา token
จาก Graph Explorer มาใส่ตรง ๆ

**`ไม่มีอะไรให้โพสต์ (ข่าวทั้งหมดเคยโพสต์ไปแล้ว)`**
ปกติ แปลว่าไม่มีข่าวใหม่ในรอบนั้น ลองเพิ่ม `MAX_AGE_HOURS` เป็น 72
หรือเพิ่มแหล่งข่าวใน `feeds.yaml`

**Gemini ตอบ 429**
เกินโควตา free tier ในนาทีนั้น ระบบจะ retry ให้เอง 3 ครั้ง ถ้ายังไม่ได้ให้ลด
`POSTS_PER_RUN` ลง หรือเปลี่ยนไปใช้ `gemini-3.5-flash-lite`

**GitHub Actions ไม่รันตามเวลา**
GitHub จะปิด scheduled workflow อัตโนมัติถ้า repo ไม่มี commit เลย 60 วัน
ซึ่งกรณีนี้ไม่เกิด เพราะบอท commit ไฟล์ `state/posted.json` กลับมาทุกครั้งที่โพสต์
อีกอย่างคือ cron ของ GitHub อาจคลาดเคลื่อนจากเวลาที่ตั้งไว้ 5-15 นาที ถือว่าปกติ

---

## ข้อควรระวังเรื่องนโยบาย

- **ห้าม commit `.env` หรือ token ขึ้น GitHub** ถ้าเผลอทำ ให้รีบไปกด
  Revoke ที่ https://www.facebook.com/settings?tab=business_tools แล้วสร้างใหม่
- โพสต์เป็นการ **สรุปข่าวด้วยคำพูดของตัวเอง + ลิงก์ให้เครดิตต้นทาง** ซึ่งเป็นวิธี
  มาตรฐานของเพจข่าว แต่อย่าแก้ prompt ให้ก๊อปเนื้อหาต้นฉบับมาทั้งดุ้น
- Facebook ไม่ชอบเพจที่โพสต์ถี่เกินไป **วันละ 2-3 โพสต์กำลังดี** อย่าเร่งเป็น 10
- ควรแวะดูโพสต์สัปดาห์ละครั้ง เผื่อ AI เขียนอะไรเพี้ยน จะได้แก้ `style.md` ทัน
